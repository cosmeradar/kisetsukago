# 季節かご（kisetsukago.com）指示書

このファイルは Claude Code への指示書です。**作業を始める前に必ず全文を読んでください。**
ここに書かれた方針は、技術的な都合より優先されます。

---

## 0. 作業前チェック（毎回）

- **毎朝ボットがこのリポジトリに自動コミットします。** 作業前に必ず `git pull` してください。
  ローカルが古いまま push すると衝突します。
- **生成物（`index.html` と `kion/index.html`）を手で書き換えないでください。** 翌朝の自動実行で上書きされます。
  直すのは必ず `scripts/build.py` の側です。
- **楽天の3つのキーはローカルにありません。** GitHub Secrets にだけ入っています。
  ローカルで楽天 API を叩くテストはできません（対処法は §8）。
- **作業範囲はこのフォルダの中だけ**です。パソコンの他の場所は触らないでください。

---

## 1. このプロジェクトは何か

気象庁の公開データ（JSON）を毎日読み、天気・季節条件に応じた商品を紹介する日本語サイト。
日本国内向け。運営者は個人1名。

**この仕組みは「人手をかけずに毎日動き続けること」を最優先に設計しています。**
判断に迷ったら「人手をかけずに毎日回り続けるか」を基準にしてください。

- 公開先: https://kisetsukago.com
- リポジトリ: `kisetsukago`（GitHub）
- 置き場: GitHub Pages（ドメイン管理は Cloudflare）
- 時計: GitHub Actions（毎日深夜に自動実行）
- 費用: **従量課金の API キーは使いません。追加クレジットも買いません。枠を超えたら止まる設定のままにします。**

---

## 2. 絶対に守るルール

破る提案はしないでください。緩める必要が出たときは、必ず先に運営者の承認を取ってください。

1. **災害・健康について断定を書かない。**
   「台風のときはこうしてください」「熱中症を防げます」といった**指示・効果の断定は禁止**です。
   サイトの役目は「データの表示」と「商品の紹介」に限定します。
   気象庁・自治体の公式情報へのリンクを各ページに必ず置きます。
2. **地域名を差し替えただけの類似ページを量産しない。**
   「東京の猛暑対策」を47都道府県分作るような構成は禁止です。
   全国分のデータは取りますが、**ページは増やしません。**
3. **翌日に無価値になる日付付き記事を毎日新規作成しない。**
   「毎日更新」＝ページを増やすことではなく、**既存ページの中身を毎日入れ替えること**です。
   蓄積記事（読み物）は週1〜2本だけゆっくり追加します。
4. **公開前の機械チェックを必ず通す。**
   データが空／気温が異常値／商品0件／禁止表現あり → **公開せずに異常終了**。
5. **気象庁の JSON は非公式で、予告なく仕様が変わりえます。**
   壊れたときに気づける仕組み（チェック＋ワークフローの失敗）を必ず維持してください。
6. **ページの題名に季節や条件を入れない。**
   過去に「猛暑ページ」を作って冬に破綻した経緯があります。
   題名は「都道府県別の気温」のような**観測項目名**にし、中身（並び順・商品）を季節で切り替えます。

---

## 3. 収益化の方針

- **楽天アフィリエイト1本のみ。** 楽天ウェブサービスの商品検索 API で、商品情報とリンクを機械生成します。
- **Amazon は当面使いません。**

---

## 4. いまのファイル構成

```
kisetsukago/
├── CNAME                       # kisetsukago.com
├── CLAUDE.md                   # このファイル
├── index.html                  # トップページ（自動生成・手で触らない）
├── kion/index.html             # 都道府県別の気温ページ（自動生成・手で触らない）
├── README.md
├── scripts/
│   ├── build.py                # ★本番。これだけが使われている（約30KB）
│   ├── heat.py                 # 旧・試作。未使用。削除してよい
│   └── survey_amedas.py        # 調査用の使い捨て。未使用
└── .github/workflows/
    ├── daily.yml               # ★本番。毎日の自動実行
    ├── jma-test.yml            # 調査用の使い捨て。未使用
    └── survey-amedas.yml       # 調査用の使い捨て。未使用
```

**注意:** 過去に `build.py` と `build2.py` が併存し、実際に動いていたのは `build2.py` なのに
`build.py` を編集していた、という事故がありました。現在は `build.py` に統合済みです。
**似た名前のファイルを増やさないでください。**

**まだ無いもの:** `robots.txt`、`sitemap.xml`、404ページ、蓄積記事用のフォルダ。

---

## 5. build.py の構造

先頭から、定数 → 取得 → 集計 → 地図 → 検査 → HTML部品 → 描画 → main の並びです。

### 主な定数

| 名前 | 意味 |
|---|---|
| `LATEST_TIME` / `MAP_TMPL` | 気象庁アメダスの最新時刻と観測値 |
| `STATION_TABLE` | 観測所の一覧（地点名を引く） |
| `FORECAST_AREA` | 府県予報区の一覧。**これで観測所を47都道府県に束ねている** |
| `RAKUTEN` | `https://openapi.rakuten.co.jp/ichibams/api/IchibaItem/Search/20260701` |
| `GA_ID` | `G-C46GBVZFLL`（Google Analytics） |
| `MAX_AGE_MINUTES` | 180。これより古いデータなら公開しない |
| `POOL_SIZE` / `SHOW_PER_KEYWORD` | 楽天から20件取り、その日の3件を選ぶ |
| `RAKUTEN_INTERVAL_SEC` | 3。**縮めると429エラーが出ます** |
| `SEASONS` | 4〜9月＝夏（高い順）、10〜3月＝冬（低い順）。商品キーワードもここ |
| `BANNED` | 禁止表現の一覧 |
| `PREF` | 都道府県コード→名前 |
| `TILE_MAP` | マス目地図の配置（13列×10行、座標直書き） |
| `C_COLD` / `C_MID` / `C_HOT` | 地図の色。和紙風の配色に合わせた薄い寒色→暖色 |

### 主な関数

- `fetch_weather()` — 最新時刻を読み、その時刻の観測値・観測所表・予報区表をまとめて取る
- `stations_by_pref()` — 予報区コードの**頭2桁**で47都道府県に集約（北海道・沖縄も統合済み）
- `summarize()` — 各県の代表地点（最高または最低）を1つ選び、並べ替える
- `fetch_pool()` / `pick_daily()` — 楽天からレビュー件数上位20件を取り、**日付を種にした固定乱数**で3件選ぶ
  （同じ日なら何度実行しても同じ商品が出る＝再現性がある）
- `heat_color()` / `japan_map_svg()` / `legend_svg()` — その日の全国最低〜最高で色を割り振る。季節設定は不要
- `run_checks()` — 下記の検査。1つでも引っかかれば `sys.exit(1)`
- `prose_text()` — 本文。**ここが禁止表現チェックの対象**
- `render_kion()` / `render_top()` — HTML を組み立てる

### 公開前の検査（run_checks）

- データが `MAX_AGE_MINUTES`（180分）より古い
- 都道府県が47にならない
- 気温が取れている地点が500未満
- 気温が取れない県が6県以上
- 気温が −50〜50℃ の外
- 地図のマス目に重複・欠落・座標の重なり
- 商品が0件／リンクや価格が不正
- **本文に禁止表現**
  （商品名は対象外。店舗が付けた名前に「熱中症対策」等が普通に入り、毎回引っかかってしまうため。
  商品名は店舗が付けたものである旨をページに明記しています）

---

## 6. 表示の設計

- **ページの題は観測項目名**（例:「都道府県別の気温」）。季節や条件を入れない。
- **各ページの中に全国の該当地域を表形式で持つ。** 地域別ページは作らない。
- **マス目地図**（13列×10行）で47都道府県を色分け。色はその日の全国最低〜最高で自動配分。
- トップページには**文字なしの小さい地図**、詳細ページには**文字と数字入りの大きい地図**。
  詳細ページの各マスは押すと表の該当行（`#p{都道府県コード}`）へ飛びます。
- 並び順は**日付で切り替え**（4〜9月は高い順、10〜3月は低い順）。
  その日のデータで切り替える方式は、境目の時期に落ち着かないため採用していません。

---

## 7. daily.yml の動き

```
cron: "20 7 * * *"   # UTC = 米国東部 夏時間 3:20 / 冬時間 2:20
```

1. `python3 scripts/build.py` を実行（Secrets を環境変数で渡す）
2. 検査で落ちればここで失敗し、**前日のページがそのまま残る**
3. 通れば `index.html` と `kion/` をコミットして push
4. 内容に変化がなければコミットしない

コミット名義は `kisetsukago-bot`（GitHub の bot 用アドレス）。

GitHub Secrets に入っているもの:
`RAKUTEN_APP_ID` / `RAKUTEN_ACCESS_KEY` / `RAKUTEN_AFFILIATE_ID`

**GitHub の cron は遅延・スキップされることがあります。**「毎日必ず動く」前提のコードは書かないでください。

---

## 8. ローカルでのテストのしかた

楽天のキーはローカルにありません。以下のどちらかで進めてください。

**A. 気象庁部分だけ試す**
`fetch_weather()` 以降の集計・地図・HTML描画は楽天なしで動きます。

**B. 疑似データモードを入れる（推奨・要承認）**
`RAKUTEN_MOCK=1` のとき `fetch_pool()` が固定の架空データを返すようにすると、
ローカルで全体を通して確認できます。本番の挙動は変わりません。
入れる前に運営者に一言確認してください。

**GitHub Actions で試す場合**
`workflow_dispatch` があるので手動実行できます。ただし本番と同じく push するので、
試験用に「書き出すが push しない」ワークフローを別に作るほうが安全です。

---

## 9. 次にやること

### 決まっているもの（優先順）

1. **観測項目ページを4枚追加する** — 雨量・風速・湿度・積雪。すべて気温ページと同じ形式にする。
   調査済み: 気温・1時間雨量・風速・風向・湿度は47都道府県すべてで取得可能。
   **積雪だけは8月時点で検証できていません。冬に再確認が必要です。**
   台風・花粉・黄砂・紫外線は、この5枚が終わるまで着手しません。
2. **不要ファイルの掃除** — `heat.py`、`survey_amedas.py`、`jma-test.yml`、`survey-amedas.yml`
3. **robots.txt と sitemap.xml** — 現在どちらも無い
4. **蓄積記事** — 週1〜2本。まだ1本もない

### まだ決まっていないこと（勝手に決めない）

- **停止時の検知方法。** いまワークフローが失敗しても、運営者は気づけません。
  GitHub の通知メールで足りるのか、別の手段が要るのか未決定です。
- **成功の判定基準。** 何をもって「自動運用が成立した」とするかが未定義です。

この2つは提案してよいですが、**決めるのは運営者です。**

---

## 10. 技術的な落とし穴（実際に踏んだもの）

- **楽天API は `Referer: https://kisetsukago.com/` と `Origin: https://kisetsukago.com` の両方が必要。**
  片方でも欠けると `HTTP_REFERRER_MISSING` の403になります。
- **楽天API を連続で叩くと429。** キーワード間に3秒空けてください。
- **気象庁のデータは GitHub Actions から取ると新鮮**（15分以内）。
- **ワークフローは `.github/workflows/` の中でないと無視されます**（リポジトリ直下では静かに無視される）。
- **`raw.githubusercontent.com` は 200 を返しても古い内容のことがあります。**
  確実に見たいときは `api.github.com` の contents API と突き合わせてください。

---

## 11. 役割分担

- **判断・設計・コード・文章はすべて Claude が行います。**
- **運営者はファイルを書きません。** 承認、ID の提供、必要に応じた画面操作だけです。
- 大きな設計変更（ページ構成、公開の可否、チェックの緩和）は、**実装前に承認を取ってください。**

---

## 12. 運営者への話しかた

- **専門用語を避ける。** 使う場合は必ず言い換えを添える。
- **一度に複数の論点を並べない。** 一つずつ順番に。
- **確認の質問は1回の返答につき1つまで。**
- **不確かなことは断定せず、「確認が必要」と明示する。**
- 時刻はすべて**米国東部時間（ET）**で書く。日本時間への変換は不要。
- 運営者は日本語話者。サイトは日本語で日本向け。

---

## 13. 用語

| 語 | 意味 |
|---|---|
| アメダス | 気象庁の自動観測網。全国約1300か所 |
| 府県予報区 | 気象庁が予報を出す区域。頭2桁で束ねて47都道府県にしている |
| kion/ | 都道府県別の気温ページの置き場 |
| 蓄積記事 | 毎日更新しない、書き足していく読み物 |
| Secrets | GitHub に預けた秘密の設定値。中身は画面上でも見えない |
